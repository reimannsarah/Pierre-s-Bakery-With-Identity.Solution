@{
  Layout = "_Layout";
}

@model Bakery.Models.Treat

<h3>Create a Treat</h3>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <input type="submit" value="Add" />
}